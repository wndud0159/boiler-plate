import React from 'react'
import { withRouter } from 'react-router-dom'

function AdminPage() {
    return (
        <div>
            어드민페이지에용
        </div>
    )
}

export default withRouter(AdminPage)
